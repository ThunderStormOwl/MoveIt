import '../styles/global.css'

import { ChallengesProvider } from '../contexts/ChallengesContext'

function MyApp({ Component, pageProps }) {
  return (
    <Component {...pageProps} />
  );
}

export default MyApp

/**
 * Challenges:
 * 
 *  - Document the project(Technologies used, how to run locally, how does it work, take some prints and gifs(maybe))
 *  - 
 * 
 */