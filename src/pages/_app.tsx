import '../styles/global.css';

function MyApp({ Component, pageProps }) {
  return (
    <Component {...pageProps} />
  );
}

export default MyApp

/**
 * Challenges:
 * 
 *  - Document the project(Technologies used, how to run locally, how does it work, take some prints and gifs(maybe)).
 *  - Better the style.
 *  - Make it more responsive(resizable).
 *  - Check figma for version 2.0 and try to implement it!
 *  - Make a sidebar.
 *  - Youtube "Serverless com ReactJS e Next.js na Vercel | Code/Drops #54" to see how to implement log in.
 *  - Rank users.
 * 
 */