import {BrowserRouter, Route, Switch, Redirect} from 'react-router-dom';



export function Routes(){
    return(
        <BrowserRouter>
            <Switch>
                <Route />
            </Switch>
        </BrowserRouter>
    )
}